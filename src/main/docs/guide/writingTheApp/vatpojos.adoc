Just as in the other guide, we'll first need to create a POJO to encapsulate the validation request:

[source,json]
----
{
  "memberStateCode": "es",
  "vatNumber": "B86412491"
}
----

[source,java]
.vat-validator-graal/src/main/java/example/micronaut/VatValidationRequest.java
----
include::{sourceDir}/vat-validator-graal/src/main/java/example/micronaut/VatValidationRequest.java[]
----

<1> Be sure to use the `@Introspected` annotation, otherwise this will throw Jackson errors in GraalVM


Then we'll add another POJO to encapsulate the expected response. Note: we add a `Boolean valid` property.

[source,json]
----
{
  "memberStateCode": "es",
  "vatNumber": "B86412491",
  "valid": true
}
----

[source,java]
.vat-validator-graal/src/main/java/example/micronaut/VatValidation.java
----
include::{sourceDir}/vat-validator-graal/src/main/java/example/micronaut/VatValidation.java[]
----